spring:
  application:
    name: order-service

# apollo config
apollo:
  meta: http://localhost:8080
  bootstrap:
    enabled: true
    namespaces: application
    eagerLoad:
      enabled: true
    refreshInterval: 5
env: DEV
app:
  id: ${spring.application.name}